import React from 'react';

const Home = () => (
  <div>
    <header>Things you can do if logged-in</header>
    <div>
      <ul>Register your works</ul>
      <ul>Display all works registered</ul>
    </div>
    <header>Things you can do if not registered</header>
    <div>
      <ul>Register ;)</ul>
    </div>
  </div>
);

export default Home;