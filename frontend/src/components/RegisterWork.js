import React from 'react';
import RegisterWork from '../containers/RegisterWork';

const getRegister = (user, navigator) => <RegisterWork user={user} navigator={navigator} />;

export default getRegister;
